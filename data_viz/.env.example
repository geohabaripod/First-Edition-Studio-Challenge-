# Copy this file to .env and fill in real values.
# NEVER commit the real .env file or ship these credentials to the browser —
# only this backend process should ever see them.


# --- Database (Neon PostgreSQL) ---
DATABASE_URL=postgresql://neondb_owner:npg_68jkdnVmlKBv@ep-morning-unit-a5d05ki0-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require

PGHOST=ep-morning-unit-a5d05ki0-pooler.us-east-2.aws.neon.tech
PGPORT=5432
PGDATABASE=neondb
PGUSER=neondb_owner
PGPASSWORD=npg_68jkdnVmlKBv
# Neon requires SSL — must be 'require', not blank/prefer
PGSSLMODE=require

# --- API server ---
# Port the API listens on
PORT=4000

# Comma-separated list of origins allowed to call this API (your frontend's origin)
CORS_ORIGIN=http://127.0.0.1:5500

# --- Database (SupaBase) ---
SUPABASE_DATABASE_URL=postgresql://postgres.qoghyocggrjixrqrpobx:AOuLI5O1L8iSbdRb@aws-0-eu-central-1.pooler.supabase.com:5432/postgres

SUPABASE_SSL_INSECURE=true
