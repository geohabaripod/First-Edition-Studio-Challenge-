# --- Database (local PostgreSQL) ---
DATABASE_URL=postgresql://postgres:Korowe123-@localhost:5432/nairobifdb?sslmode=prefer&connect_timeout=10

PGHOST=localhost
PGPORT=5432
PGDATABASE=nairobifdb
PGUSER=postgres
PGPASSWORD=Korowe123-
# 'prefer' (not 'require') so db.js connects without forcing SSL locally
PGSSLMODE=

# --- API server ---
# Port the API listens on
PORT=4000

# Comma-separated list of origins allowed to call this API (your frontend's origin)
CORS_ORIGIN=http://127.0.0.1:5500
