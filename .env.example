# Copy this file to .env and fill in real values.
# NEVER commit the real .env file or ship these credentials to the browser —
# only this backend process should ever see them.

PGHOST='ep-lucky-union-b1noitqk-pooler.c-5.eu-central-1.aws.neon.tech'
PGDATABASE='neondb'
PGUSER='neondb_owner'
PGPASSWORD='npg_C74JRQikaAMw'
PGSSLMODE='require'
PGCHANNELBINDING='require'

# Port the API listens on
PORT=4000

# Comma-separated list of origins allowed to call this API (your frontend's origin)
CORS_ORIGIN=http://127.0.0.1:5500
